@using QuitSmoking.Repositories.HoangNV.Models;
@using QuitSmoking.Repositories.HoangNV.ModelExtentions;
@model PaginationResult<PlanQuitMethodHoangNv>

@{
    ViewData["Title"] = "Index";
     var planId = Context.Request.RouteValues["id"];
}

<h1>Index</h1>

<p>
    <a asp-action="Create" asp-route-id="@planId">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Start Date</th>
            <th>End Date</th>
            <th>Is Successful</th>
            <th>User Rating</th>
            <th>User Notes</th>
            <th>Motivation Reason</th>
            <th>Method Description</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Items) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsSuccessful)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserRating)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserNotes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatePlanQuitSmokingHoangNv.MotivationReason)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuitMethodHoangNv.MethodDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlanQuitMethodHoangNvid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PlanQuitMethodHoangNvid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PlanQuitMethodHoangNvid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
